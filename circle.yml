machine:
  services:
    - docker

dependencies:
  post:
    - ./download-meshes

test:
  override:
    - |
        docker run \
          -v $(pwd):/home/fenics/fenapack \
          -w /home/fenics/fenapack \
          quay.io/fenicsproject/stable \
          "export PYTHONPATH=\$PWD:\$PYTHONPATH; cd test/regression; python test.py"
