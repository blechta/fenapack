First you need to be able to import FENaPack.

To generate API doc, build HTML and commit it to gh-pages branch::

    make gh-pages
    git push origin gh-pages

FIXME: The drawback is that all untracked/ignored files are lost
during this procedure. So be careful!
